# node-subBuild
用于简单运行自动化打包

```
/*
0、登录后，才能操作，每次操作都得判断是否过期了
1、写一个定时任务器，进行启动着。
    1. 每过一段时间将登录的token更改。
    2. 每次登录后都得更改下登录的token
2、项目有两种，一种是前端页面打包的，第二种是跑 node服务。
3、新建任务，存储在本地（任务名称、任务简介、任务执行步骤）
    1. 每次通过本地加载出来任务列表
    2. 每次通过前端运行按钮，执行某任务里面的命令步骤
    3. 

1、运行node 后台服务的，
*/

```

